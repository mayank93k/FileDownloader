package scala.spark.org.common.constant

object ApplicationConstant {
  val BaseUrl = "" // URL to download file
  val DownloadDirectory = "downloads"
  val FileLinkPattern = """href="([^"]*?\.(zip|csv|txt))""""
  val DatePattern = """href="(\d{4}-\d{2}/)""""
}
